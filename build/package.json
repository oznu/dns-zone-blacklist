{
  "name": "bind-zone-blacklist",
  "version": "1.0.0",
  "description": "A tool to convert host file data to bind dns entries for use with oznu/dns-ad-blocker.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "oznu <dev@oz.nu>",
  "license": "MIT",
  "dependencies": {
    "request": "^2.78.0",
    "request-promise": "^4.1.1"
  }
}
